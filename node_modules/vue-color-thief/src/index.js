import ColorThief from './libs/color-thief-master/dist/color-thief.min.js';

console.log(ColorThief);

const VueColorThief = {
  install(Vue, options) {
    if(!options) {
      return;
    }
  }
}

export default VueColorThief;
